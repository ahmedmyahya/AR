<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Upload Model - 3D Viewer</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body class="<%= user && user.theme === 'dark' ? 'dark' : '' %>">
    <%- include('partials/header') %>

    <main class="container">
      <h1>Upload New 3D Model</h1>

      <% if (error) { %>
      <div class="error-message"><%= error %></div>
      <% } %>

      <form
        action="/upload"
        method="POST"
        enctype="multipart/form-data"
        class="model-form"
      >
        <div class="form-group">
          <label for="name">Model Name:</label>
          <input type="text" id="name" name="name" required />
        </div>
        <div class="form-group">
          <label for="tags">Tags (comma-separated, e.g., car, vehicle):</label>
          <input
            type="text"
            id="tags"
            name="tags"
            placeholder="Optional tags"
          />
        </div>
        <div class="form-group">
          <label for="modelFile"
            >Choose 3D Model File (.glb, .gltf, .obj, .fbx, .zip, .rar):</label
          >
          <input
            type="file"
            id="modelFile"
            name="modelFile"
            accept=".glb,.gltf,.obj,.fbx,.zip,.rar"
            required
          />
        </div>
        <button type="submit" class="button">Upload Model</button>
      </form>
    </main>

    <%- include('partials/footer') %>

    <script src="/js/theme-toggle.js"></script>
  </body>
</html>
